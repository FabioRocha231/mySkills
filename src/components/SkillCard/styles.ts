import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    buttonSkill: {
        backgroundColor: "#1F1E25",
        padding: 15,
        borderRadius: 30,
        marginTop: 10,
      },
      textSkill: {
        color: '#FFF',
        fontSize: 18,
        fontWeight: 'bold',
        textAlign: 'center'    
      }
})

export { styles }